# Server
PORT=3001
NODE_ENV=development

# Database (Railway provides DATABASE_URL)
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secrets (generate strong secrets - minimum 32 characters)
# Use a tool like: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET=your-access-secret-min-32-chars-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-secret-min-32-chars-change-this-in-production
JWT_ACCESS_EXPIRES_IN=30m
JWT_REFRESH_EXPIRES_IN=30d

# CORS
CORS_ORIGIN=http://localhost:3000

# LLM Provider Encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-encryption-key-generate-with-command-above

# For production on Railway, these are auto-set:
# RAILWAY_STATIC_URL, RAILWAY_PUBLIC_DOMAIN
